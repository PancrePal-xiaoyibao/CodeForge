interface:
  display_name: "Dev Host Init"
  short_description: "宿主机一键初始化：环境探测 + 缺口提醒 + codebase-mcp 部署提醒 + 分层注入体系生成（规则主文档 + 外置参考 + pi 强化层）"
  default_prompt: "使用 $dev-host-init 把本机初始化为合格开发主机：探测环境、提醒补装缺失工具、部署 codebase-memory-mcp、生成分层注入体系（规则主文档+agent-reference+pi 强化层）。"
